build/
dist/
__pycache__/
*.pyc
.env
*.spec
!ontrack.spec
!buildozer.spec
!installer/installer.spec
tags
.tags

# Build / runtime logs (these often capture secrets such as cargo / pip
# tokens from the environment — never commit them).
*.log
log.txt
buildozer_debug.log
.buildozer/
bin/
