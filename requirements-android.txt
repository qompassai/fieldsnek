# /qompassai/ONTrack/requirements-android.txt
# Android-only pip requirements (mirrors `requirements =` in buildozer.spec).
# Heavy desktop deps (faster-whisper, ctranslate2, ortools, sounddevice, pandas,
# numpy, pyproj, shapely) are intentionally absent — they have no p4a recipe.
android
certifi
chardet
charset-normalizer
idna
kivy==2.3.0
pillow
plyer
pyjnius
python-dotenv
requests
urllib3
